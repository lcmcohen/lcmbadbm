/**
 * A container for all elements used in the construction of the badBM instance UI.
 * These elements are the mainFrame for, which all other elements are clipped to,
 * The run panel, for displaying the results of previous runs,
 * and the SelectFrame, which allows exploration through the branches of a specified directory.
 */
package edu.touro.mco152.bm.ui;